name: Build dlgTool AJ Android Edition

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore NuGet packages
        run: nuget restore dlgTool-AJ-Android-Edition.sln

      - name: Build Release Solution
        run: msbuild dlgTool-AJ-Android-Edition.sln /p:Configuration=Release /p:Platform="Any CPU"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dlgTool-build
          path: |
            **/bin/Release/*.exe
            **/bin/Release/*.dll
